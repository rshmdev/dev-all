import styles from "@/styles/Header.module.css";
const Header = () => {
  return (
    <header className={styles.Header}>
      <h1 className={styles.dev_all}>dev / ALL</h1>
    </header>
  );
};

export default Header;
